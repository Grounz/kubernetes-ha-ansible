{
    "CN": "{{ ansible_hostname }}",
    "hosts": [
        "{{ ansible_default_ipv4.address }}",
        "{{ ansible_hostname }}"
    ],
    "key": {
        "algo": "ecdsa",
        "size": 256
    },
    "names": [
        {
            "C": "US",
            "L": "CA",
            "ST": "San Francisco"
        }
    ]
}
